import Offline from '../../screens/Offline.jsx';

export const metadata = {
  title: 'Offline | Aries 11 Bakehouse',
  robots: { index: false, follow: false },
};

export default function OfflinePage() {
  return <Offline />;
}
