node_modules/
.next/
.vercel/
.env
.env.local
.env.*.local
*.log
.DS_Store

# Machine-local AI-tool hook configs (absolute paths, not portable)
.codex/
.cursor/
.claude/
.impeccable/

# Design source/reference material — not served by the app. The runtime
# uses curated copies in public/uploads.
brand/
.vercel
.env*

# Local Supabase CLI state from `supabase link` (project ref, versions) —
# machine-local, not project source.
supabase/.temp/
